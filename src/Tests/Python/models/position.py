class Position():
	def __init__(symbol, name, quantity, average_price, last_price):
		self.symbol = symbol
		self.name = name
		self.quantity = quantity
		self.average_price = average_price
		self.last_price = last_price